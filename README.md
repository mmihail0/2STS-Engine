## Pretext
A semi-Thue system, at its core, is a string rewriting system using a finite alphabet. Since string rewriting systems are Turing complete, then so must be this kind of system. Using Python, I will create a semi-Thue system, and then use that system to create another semi-Thue system. 
